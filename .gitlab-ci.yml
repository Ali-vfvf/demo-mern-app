image: node:22

stages:
  - unit-tests
  - build

unit-tests:
  stage: unit-tests
  script:
    - cd backend
    - npm install
    - node test.js

build:
  stage: build
  script:
    - docker compose build